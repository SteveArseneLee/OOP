/*#include <iostream>
using namespace std;
int main(){
	/*int a = 0, b = 0;
	cout << "Please enter the first number : ";
	cin >> a;
	cout << "Please enter the second number : ";
	cin >> b;
	cout << a << " + " << b << " = " << a + b << endl;
	cout << a << " - " << b << " = " << a - b << endl;
	cout << a << " * " << b << " = " << a * b << endl;
	cout << a << " / " << b << " = " << a / b << endl;
	cout << a << " % " << b << " = " << a % b << endl;

	int i1 = 2, i2 = 5, i3 = -3;
	double d1 = 2.0, d2 = 5.0, d3 = -0.5;
	cout << i1 + i2 << endl; // 7
	cout << i1 / i2 << endl; // 0
	cout << i2 / i1 << endl; // 2
	cout << i1 * i3 << endl; // -6
	cout << d1 + d2 << endl; // 7
	cout << d1 / d2 << endl; // 0.4
	cout << d2 / d1 << endl; // 2.5
	cout << d3 * d1 << endl; // -1
	cout << d1 + i2 << endl; // 7
	cout << i1 / d2 << endl; // 0.4
	cout << d2 / i1 << endl; // 2.5
	cout << i2 / d1 << endl; // 2.5
	cout << i1 / i2 * d1 << endl; // 0
	cout << d1 * i1 / i2 << endl; // 0.8
	cout << d1 / d2 * i1 << endl; // 0.8
	cout << i1 * d1 / d2 << endl; // 0.8
	cout << i2 / i1 * d1 << endl; // 4
	cout << d1 * i2 / i1 << endl; // 5
	cout << d2 / d1 * i1 << endl; // 5
	cout << i1 * d2 / d1 << endl; // 5 
	 

	int i1 = 2, i2 = 5, i3 = -3;
	double d1 = 2.0, d2 = 5.0, d3 = -0.5;
	cout << i1 + (i2 * i3) << endl; // -13
	cout << i1 * (i2 + i3) << endl; // 4
	cout << i1 / (i2 + i3) << endl; // 1
	cout << i1 / i2 + i3 << endl; // -3
	cout << 3 + 4 + 5 / 3 << endl; // 8
	cout << (3 + 4 + 5) / 3 << endl; // 4
	cout << d1 + (d2 * d3) << endl; // -0.5
	cout << d1 + d2 * d3 << endl; // -0.5
	cout << d1 / d2 - d3 << endl; // 0.9
	cout << d1 / (d2 - d3) << endl; // 0.363636
	cout << d1 + d2 + d3 / 3<< endl; // 6.83333
	cout << (d1 + d2 + d3) / 3 << endl; // 2.16667
	cout << d1 + d2 + (d3 / 3) << endl; // 6.83333
	cout << 3 * (d1 + d2) * (d1 - d3) << endl; // 52.5

	float a = 0, b = 0;
	cout << "Please enter the first number : ";
	cin >> a;
	cout << "Please enter the second number : ";
	cin >> b;
	cout << a << " + " << b << " = " << a + b << endl;
	cout << a << " - " << b << " = " << a - b << endl;
	cout << a << " * " << b << " = " << a * b << endl;
	cout << a << " / " << b << " = " << a / b << endl;

	double C, r = 1;
	const double PI = 3.14159;
	C = 2 * PI * r;
	cout << "Please enter the circle's radius : ";
	cin >> r;
	cout << "Circumference is " << C << '\n';

	int length=0, width=0;
	cout << "length : ";
	cin >> length;
	cout << "width : ";
	cin >> width;
	cout << "Perimeter : " << (length + width) * 2;

	int x = 2945;
	cout << x % 10 << endl; // 5
	cout << x / 10 % 10 << endl; // 4
	cout << x / 100 % 10 << endl; //9

	int x = 4, y = 5;
	cout << x++ + y << endl; // 9
	cout << x++ + --y << endl; // 9
	cout << ++x - y-- << endl; // 3 

	int x = 3;
	cout << 'x' + 1 << endl;

	double x1, x2, y1, y2;
	char left_paren, comma, right_paren;
	cout << "Please enter the first point : ";
	cin >> left_paren >> x1 >> comma >> y1 >> right_paren;
	cout << "Please enter the second point : ";
	cin >> left_paren >> x2 >> comma >> y2 >> right_paren;
	cout << "The midpoint of " << left_paren << x1 << comma << y1 << right_paren
		<< " and " << left_paren << x2 << comma << y2 << right_paren << " is " 
		<< left_paren << (x1 + x2) / 2 << comma << (y1 + y2) / 2 << right_paren;
	
	int a;
	while (a>1) {
			a = a % 2;
	}
	cout << a;
	
	int x = 3, y = 5, z = 7;
	bool b1 = true, b2 = false, b3 = x == 3, b4 = y < 3;
	if (x < y)
		cout << "print" << endl;
	
}
*/